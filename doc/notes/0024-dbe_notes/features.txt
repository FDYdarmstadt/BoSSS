Features not required anymore
-----------------------------
- Projects
- Polling
- "Only with session.xml"

Wishlist
------------
- Full grid handling (list, edit description, copy, move, delete, plot/export, import)
- Kommandozeile (C# Interpreter?; residual plotting using gnuplot?)
- Transactional file system (if applicable)
- Different "connectors" (Dateisystem, sftp/ssh, ...)
- Clean Database (find and remove unused files
- Lazy load databases/select databses to load
- Direct editing of DBE.xml from the dbe
- Arbitrary ordering/grouping of sessions
- Associate arbitrary files with a session
- Export/import sessions (zip archive)
- Generation of control files (restart, query)
- Progress bar
- Abort operation